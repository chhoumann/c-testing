#include <stdio.h>

int main(void)
{
    /* The code below is a part of the lecture video. */
    int n = 2;
    double a = 3.5;
    double b = 1.2;

    printf("Der er %d variable.\na + b = %-6.1f. a - b = %6.1f\n", n, a + b, a - b);

    return 0;
}