#include <stdio.h>

int main (void) {

    /*  The difference between the different data types is the amount of memory they occupy and the range of values they can hold.  */

    int rgbColor = 0xFFEF0D; /* Ints can store hexadecimals */
    float sci_not = 1.7e4;
    double same_as_above_but_bigger = 1.7e4; /* Same type as float but bigger and better */
    _Bool yes_no = 1;

    
    return 0;
}